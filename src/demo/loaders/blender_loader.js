// import {BlendLoader} from "three/examples/jsm/loaders/BlendLoader.js"
import {DebugSpecial} from "../core/debug_core";


export async function BlenderLocalLoader(app, text) {
    // const loader = new BlendLoader();
    // console.log(loader)
    // loader.parse(
    //     text, '',
    //     // called when the resource is loaded
    //     async ( gltf ) => { await LoadScene(app, gltf); },
    //     function ( error ) {console.log( 'An error happened' );},
    //     function ( xhr ) {
    //         DebugSpecial( ( xhr.loaded / xhr.total * 100 ) + '% loaded' );
    //     },
    // );
}
